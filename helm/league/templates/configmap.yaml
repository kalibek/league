apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "league.fullname" . }}
  labels:
    {{- include "league.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
