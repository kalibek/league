League app deployed.

{{- if .Values.ingress.enabled }}
URL: http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}
{{- else if eq .Values.service.type "LoadBalancer" }}
Get the external IP:
  kubectl get svc {{ include "league.fullname" . }} -n {{ .Release.Namespace }} -w
{{- else }}
Port-forward to access locally:
  kubectl port-forward svc/{{ include "league.fullname" . }} 8080:80 -n {{ .Release.Namespace }}
  open http://localhost:8080
{{- end }}

Required secret (if not yet created):
  kubectl create secret generic <existingSecret> \
    --from-literal=DATABASE_URL='postgres://user:pass@host:5432/db?sslmode=disable' \
    --from-literal=JWT_SECRET='...' \
    -n {{ .Release.Namespace }}

Set existingSecret in values.yaml or --set existingSecret=<name>.
